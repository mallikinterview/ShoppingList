// Namespaces every test file needs. Declared once here rather than repeated at the top of
// each file — this is the case global usings exist for.

// AwesomeAssertions is a community fork of FluentAssertions v7, created after FluentAssertions
// v8 moved to a commercial licence. It is a drop-in replacement and deliberately keeps the
// original namespace, so assertions read identically and a future swap costs nothing.
global using FluentAssertions;
global using Xunit;
