{
  "//": "Development overrides. Still no secrets: when running outside Docker, supply connection strings and keys via `dotnet user-secrets` (UserSecretsId is set in the csproj). The Section__Key names used there are identical to the environment variable names, so the same configuration works in both places without translation.",

  "SerilogSettings": {
    "MinimumLevel": "Debug"
  },

  "KeycloakSettings": {
    "RequireHttpsMetadata": false
  }
}
