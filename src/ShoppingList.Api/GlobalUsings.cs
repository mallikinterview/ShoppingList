// Namespaces referenced by nearly every file. Kept deliberately short — a long global using
// list makes it hard to see what a file actually depends on, which is the opposite of the point.

global using ShoppingList.Api.Common.RateLimiting;
